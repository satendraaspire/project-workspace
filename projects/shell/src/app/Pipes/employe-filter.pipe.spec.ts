import { EmployeFilterPipe } from './employe-filter.pipe';

describe('EmployeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
